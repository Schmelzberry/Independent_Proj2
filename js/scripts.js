window.onload = function() {
    const form = document.querySelector("form");
    form.onsubmit = function (event) {
        event.preventDefault();

        let languageReveal = document.getElementById("languages");
        languageReveal.setAttribute("class", "hidden");
        let pythonInfo = document.getElementById("pyth");
        pythonInfo.setAttribute("class", "hidden");

        const catdog = parseInt(document.querySelector("input#catdog").value);

        if (catdog >= )
    }
}




